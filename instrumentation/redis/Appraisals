# frozen_string_literal: true

if Gem::Version.new(RUBY_VERSION) < Gem::Version.new('4')
  appraise 'redis-4.x' do
    gem 'redis-client', '~> 0.22'
    gem 'redis', '~> 4.8'
  end
end

%w[5.0.0 6.0.0].each do |version|
  appraise "redis-#{version}" do
    gem 'redis', "~> #{version}"
  end
end
